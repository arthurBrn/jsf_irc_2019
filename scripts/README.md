Run './init' at the root directoy to install all dependencies for angular and node and will start servers

In script folder you can run the server by executing './start_servers'
and stop them with './stop_servers'